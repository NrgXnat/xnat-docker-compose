# Node identity for multi-node XNAT. Each node needs a unique node.id so the Node/Task
# framework (SingleNodeExecutionResolver) runs scheduled tasks on only one node.
node.id=xnat-web-2
xnat.is_primary_node=false
